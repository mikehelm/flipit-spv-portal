/**
 * Footer attribution — BUILD_SPEC §13.2.
 *
 * "Subtle is the requirement, not a nicety." Small, dimmed, below everything
 * else, no logo, no colour, no animation. It appears on the admin side and the
 * investor portal, and deliberately never on the invitation email or the
 * participation certificate.
 */
export function SiteFooter() {
  return (
    <footer className="border-t hairline px-5 py-4 text-[11px] leading-relaxed text-[#9498b5]">
      <p>Made by Make with Mike</p>
    </footer>
  )
}
