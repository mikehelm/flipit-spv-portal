node_modules
.next
.env
.env.local
*.log
.DS_Store
coverage

# build artefacts — must never be committed, they conflict on every merge
*.tsbuildinfo
backups/

# uploaded media for MEDIA_STORE="filesystem" — never committed
.media/
